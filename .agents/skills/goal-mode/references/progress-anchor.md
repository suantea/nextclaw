# 目标进度锚点

默认落点为当前有效迭代的 `work/goal-progress.md`，内容包含：当前目标、验收条件、非目标、冻结边界、已完成进展、当前下一步和计数器。

- 默认不超过 50 行，绝不超过 100 行。
- 只保留当前真相；过期结论直接替换，不写历史流水账。
- `0/20` 是新周期起点，先回看计划；`20/20` 是周期终点，回看后重置为 `0/20`。
- 用户指出偏航、开始新抽象、连续讨论不推进或上下文恢复时立即回看，不等待计数到 20。
- 文件状态与用户可见计数器保持一致。
