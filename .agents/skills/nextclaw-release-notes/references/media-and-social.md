# 版本配图与社交

- 图片必须真实、公开安全并直接证明主结论；空态、mock、加载态、被裁断结果或弱装饰图不采用。
- 用户可见 UI、真实工作流、结果形态和新入口优先真实产品截图；纯 CLI、协议、稳定性、内部链路默认无图。
- release summary 返回的候选图逐项判断；采用时复制为版本稳定资产，中英文页面使用匹配 locale。
- Stable minor 在公开 release note URL 返回 200 后默认发布一条 X 短帖；patch 和内部治理默认不发，major 版本单独确认宣发范围。
- 发布前先检索最近一次成功 stable minor 的迭代记录，复用已验证的 `x-bird` 命令、Node/代理参数和回读方式；没有证据表明旧路径失效时，不另造发布链路。
- X 帖默认带一张高信息密度图片，但不固定单一形式：可选择真实产品截图、版本/数据摘要图、AI 生成的概念视觉，或真实截图与 AI 视觉包装的组合。按本次主结论选择最能传达价值的形式；功能可见行为优先真实截图，性能数据优先带口径的摘要图，抽象能力或品牌叙事可使用 AI 生图。概念图和组合图不得冒充产品截图，不得生成或改写不存在的 UI、测量结果和产品状态。
- 写入 release review 前，必须使用 `nextclaw-product-visual-assets` 的“版本传播图质量门”完成近期版本去重、实际开图审查和最低分维度迭代；未通过时保持 `CONTENT_PENDING`，不得复用旧图凑齐字段。
- 用户已给出 stable minor 长期发布授权时，不再逐次请求文案确认；有授权工具时直接发布，并按返回的帖子 ID 回读核对作者、正文和媒体后记录 URL。没有能力时交付可直接发布的文案和图片路径，明确阻塞。
- 社交文案只写版本号、主要用户价值、1-2 个亮点和 release note 链接，不写内部流程。
