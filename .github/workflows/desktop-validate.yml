name: desktop-validate

on:
  pull_request:
    paths:
      - "apps/desktop/**"
      - "packages/nextclaw/**"
      - "packages/nextclaw-ui/**"
      - "packages/nextclaw-server/**"
      - "packages/nextclaw-openclaw-compat/**"
      - "packages/nextclaw-core/**"
      - "packages/nextclaw-kernel/**"
      - "packages/nextclaw-runtime/**"
      - "packages/ncp-packages/**"
      - "packages/extensions/**"
      - "pnpm-lock.yaml"
      - "pnpm-workspace.yaml"
      - "package.json"
      - "scripts/**"
      - ".github/workflows/desktop-validate.yml"
  push:
    branches:
      - master
      - main
    paths:
      - "apps/desktop/**"
      - "packages/nextclaw/**"
      - "packages/nextclaw-ui/**"
      - "packages/nextclaw-server/**"
      - "packages/nextclaw-openclaw-compat/**"
      - "packages/nextclaw-core/**"
      - "packages/nextclaw-kernel/**"
      - "packages/nextclaw-runtime/**"
      - "packages/ncp-packages/**"
      - "packages/extensions/**"
      - "pnpm-lock.yaml"
      - "pnpm-workspace.yaml"
      - "package.json"
      - "scripts/**"
      - ".github/workflows/desktop-validate.yml"
  workflow_dispatch:
    inputs:
      target:
        description: Desktop validation path to run during focused recovery
        type: choice
        default: all
        options:
          - all
          - runtime
          - macos-dmg
          - windows-exe
          - windows-installer
          - linux-package

permissions:
  contents: read

concurrency:
  group: desktop-validate-${{ github.ref }}
  cancel-in-progress: true

jobs:
  desktop-runtime-smoke:
    if: ${{ inputs.target == '' || inputs.target == 'all' || inputs.target == 'runtime' }}
    name: desktop-runtime-smoke
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.15.1

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint / Typecheck / Runtime Smoke
        run: |
          pnpm -r --filter @nextclaw/desktop... build
          pnpm --filter @nextclaw/server exec vitest run src/features/server-path/controllers/server-path.controller.test.ts
          pnpm -C apps/desktop bundle:test
          pnpm -C apps/desktop lint
          pnpm -C apps/desktop tsc
          pnpm -C apps/desktop smoke
          ELECTRON_RUN_AS_NODE=1 pnpm -C apps/desktop exec electron scripts/smoke/service-app-host.smoke.mjs

      - name: Build product bundle + signed manifest smoke
        shell: bash
        run: |
          set -euo pipefail

          OUTPUT_DIR="$(mktemp -d)"
          KEY_DIR="$(mktemp -d)"
          MINIMUM_LAUNCHER_VERSION="$(node apps/desktop/scripts/update/services/print-minimum-launcher-version.service.mjs --channel stable)"
          trap 'rm -rf "$OUTPUT_DIR" "$KEY_DIR"' EXIT

          pnpm -C apps/desktop bundle:build -- \
            --channel stable \
            --platform linux \
            --arch x64 \
            --version 0.17.6-validate \
            --minimum-launcher-version "$MINIMUM_LAUNCHER_VERSION" \
            --output-dir "$OUTPUT_DIR"

          BUNDLE_PATH="$(find "$OUTPUT_DIR" -maxdepth 1 -type f -name 'nextclaw-bundle-*.zip' | head -n 1)"
          if [ -z "$BUNDLE_PATH" ]; then
            echo "bundle archive not found"
            exit 1
          fi

          node - "$KEY_DIR/private.pem" "$KEY_DIR/public.pem" <<'NODE'
          const { generateKeyPairSync } = require('node:crypto');
          const { writeFileSync } = require('node:fs');
          const privateKeyPath = process.argv[2];
          const publicKeyPath = process.argv[3];
          const { privateKey, publicKey } = generateKeyPairSync('ed25519');
          writeFileSync(privateKeyPath, privateKey.export({ type: 'pkcs8', format: 'pem' }));
          writeFileSync(publicKeyPath, publicKey.export({ type: 'spki', format: 'pem' }));
          NODE

          pnpm -C apps/desktop bundle:manifest -- \
            --bundle "$BUNDLE_PATH" \
            --channel stable \
            --platform linux \
            --arch x64 \
            --version 0.17.6-validate \
            --minimum-launcher-version "$MINIMUM_LAUNCHER_VERSION" \
            --bundle-url "https://example.com/$(basename "$BUNDLE_PATH")" \
            --output "$OUTPUT_DIR/manifest-stable-linux-x64.json" \
            --private-key-file "$KEY_DIR/private.pem"

          node - "$BUNDLE_PATH" "$OUTPUT_DIR/manifest-stable-linux-x64.json" "$KEY_DIR/public.pem" <<'NODE'
          const { readFileSync } = require('node:fs');
          const { createPublicKey, verify } = require('node:crypto');
          const bundlePath = process.argv[2];
          const manifestPath = process.argv[3];
          const publicKeyPath = process.argv[4];
          const manifest = JSON.parse(readFileSync(manifestPath, 'utf8'));
          const publicKey = createPublicKey(readFileSync(publicKeyPath, 'utf8'));
          const manifestPayload = JSON.stringify({
            channel: manifest.channel,
            platform: manifest.platform,
            arch: manifest.arch,
            latestVersion: manifest.latestVersion,
            minimumLauncherVersion: manifest.minimumLauncherVersion,
            bundleUrl: manifest.bundleUrl,
            bundleSha256: manifest.bundleSha256,
            bundleSignature: manifest.bundleSignature,
            releaseNotesUrl: manifest.releaseNotesUrl
          });
          const manifestValid = verify(
            null,
            Buffer.from(manifestPayload),
            publicKey,
            Buffer.from(manifest.manifestSignature, 'base64')
          );
          if (!manifestValid) {
            throw new Error('manifest signature verification failed');
          }
          const valid = verify(
            null,
            readFileSync(bundlePath),
            publicKey,
            Buffer.from(manifest.bundleSignature, 'base64')
          );
          if (!valid) {
            throw new Error('bundle signature verification failed');
          }
          NODE

  desktop-macos-dmg-install-smoke:
    if: ${{ inputs.target == '' || inputs.target == 'all' || inputs.target == 'macos-dmg' }}
    name: desktop-macos-dmg-install-smoke
    runs-on: macos-14
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.15.1

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build macOS DMG
        run: |
          set -euo pipefail
          export CSC_IDENTITY_AUTO_DISCOVERY=false
          pnpm -r --filter @nextclaw/desktop... build
          pnpm --filter @nextclaw/server exec vitest run src/features/server-path/controllers/server-path.controller.test.ts
          pnpm -C apps/desktop bundle:seed -- --channel stable --platform darwin --arch arm64
          pnpm -C apps/desktop lint
          pnpm -C apps/desktop tsc
          pnpm -C apps/desktop build:main
          pnpm -C apps/desktop native-resources -- --platform darwin --arch arm64
          pnpm -C apps/desktop exec electron-builder --mac dmg --arm64 --publish never

      - name: Smoke test macOS DMG install
        run: |
          set -euo pipefail
          DMG_PATH="$(find apps/desktop/release -maxdepth 1 -type f -name '*.dmg' | head -n 1)"
          if [ -z "${DMG_PATH}" ]; then
            echo "macOS dmg not found in apps/desktop/release"
            exit 1
          fi
          apps/desktop/scripts/smoke-macos-dmg.sh "${DMG_PATH}" 120

      - name: Upload macOS smoke logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: desktop-validate-smoke-logs-macos-arm64
          if-no-files-found: warn
          path: |
            ${{ runner.temp }}/nextclaw-desktop-smoke-home/logs/**
            ${{ runner.temp }}/nextclaw-desktop-smoke-logs/**
            ~/Library/Logs/NextClaw Desktop/**

      - name: Upload macOS DMG artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: desktop-validate-macos-arm64
          if-no-files-found: warn
          path: |
            apps/desktop/release/*.dmg
            apps/desktop/release/latest-mac.yml
            apps/desktop/release/*.blockmap

  desktop-windows-exe-smoke:
    if: ${{ inputs.target == '' || inputs.target == 'all' || inputs.target == 'windows-exe' }}
    name: desktop-windows-exe-smoke
    runs-on: windows-latest
    timeout-minutes: 75
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.15.1

      - name: Install dependencies
        shell: pwsh
        run: pnpm install --frozen-lockfile

      - name: Build desktop runtime dependencies
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          pnpm -r --filter @nextclaw/desktop... build
          pnpm --filter @nextclaw/server exec vitest run src/features/server-path/controllers/server-path.controller.test.ts

      - name: Verify Windows session catalog upgrade recovery
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          pnpm --filter @nextclaw/kernel exec vitest run src/stores/ncp-agent-session-catalog-migration.store.test.ts src/stores/ncp-agent-session-summary-index.store.test.ts

      - name: Smoke Service Apps with Electron host Node
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $env:ELECTRON_RUN_AS_NODE = "1"
          pnpm -C apps/desktop exec electron scripts/smoke/service-app-host.smoke.mjs

      - name: Minimal Electron app-region smoke
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          & "apps/desktop/scripts/smoke-windows-electron-app-region-minimal.ps1"

      - name: Build Windows desktop executable
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $env:CSC_IDENTITY_AUTO_DISCOVERY = "false"
          pnpm -C apps/desktop bundle:seed -- --channel stable --platform win32 --arch x64
          pnpm -C apps/desktop lint
          pnpm -C apps/desktop tsc
          pnpm -C apps/desktop build:main
          pnpm -C apps/desktop native-resources -- --platform win32 --arch x64
          pnpm -C apps/desktop exec electron-builder --win dir --x64 --publish never

      - name: Reproduce and repair the released Windows upgrade regression
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $ErrorActionPreference = "Stop"
          $upgradeRoot = Join-Path $env:RUNNER_TEMP "nextclaw-windows-session-catalog-upgrade"
          New-Item -ItemType Directory -Path $upgradeRoot -Force | Out-Null

          function Get-ReleasedDesktopExecutable {
            param([string]$Tag, [string]$Asset)
            $releaseRoot = Join-Path $upgradeRoot $Tag
            New-Item -ItemType Directory -Path $releaseRoot -Force | Out-Null
            gh release download $Tag --repo Peiiii/nextclaw --pattern $Asset --dir $releaseRoot
            $zipPath = Join-Path $releaseRoot $Asset
            $unpackedRoot = Join-Path $releaseRoot "unpacked"
            Expand-Archive -Path $zipPath -DestinationPath $unpackedRoot -Force
            $desktopExe = Get-ChildItem -Path $unpackedRoot -Filter "NextClaw Desktop.exe" -Recurse -File |
              Select-Object -First 1
            if ($null -eq $desktopExe) {
              throw "Released Windows Desktop executable missing after extracting $Asset"
            }
            return $desktopExe.FullName
          }

          $released044Exe = Get-ReleasedDesktopExecutable "v0.44.1-desktop.1" "NextClaw.Desktop-0.0.269-win32-x64-unpacked.zip"
          $pre047Exe = Get-ReleasedDesktopExecutable "v0.45.5-desktop.1" "NextClaw.Desktop-0.0.276-win32-x64-unpacked.zip"
          $broken047Exe = Get-ReleasedDesktopExecutable "v0.47.0-desktop.1" "NextClaw.Desktop-0.0.278-win32-x64-unpacked.zip"
          $released048Exe = Get-ReleasedDesktopExecutable "v0.48.0-desktop.1" "NextClaw.Desktop-0.0.279-win32-x64-unpacked.zip"
          $candidateExe = (Resolve-Path "apps/desktop/release/win-unpacked/NextClaw Desktop.exe").Path

          $env:NEXTCLAW_DESKTOP_UPDATE_MANIFEST_URL = "https://github.com/Peiiii/nextclaw/releases/download/v0.48.0-desktop.1/manifest-stable-win32-x64.json"
          try {
            & "apps/desktop/scripts/smoke-windows-desktop.ps1" -DesktopExePath $released044Exe -StartupTimeoutSec 120 -MaxReadySec 30 -DisableGuardian -SkipExtendedProbes -SessionCatalogUpgradeProbe "seed-visible-044" -ApplyAvailableUpdate -ExpectUpdatedRuntimeFailure -RemoteDebuggingPort 9223
          } finally {
            Remove-Item Env:\NEXTCLAW_DESKTOP_UPDATE_MANIFEST_URL -ErrorAction SilentlyContinue
          }
          & "apps/desktop/scripts/smoke-windows-desktop.ps1" -DesktopExePath $candidateExe -StartupTimeoutSec 120 -MaxReadySec 30 -ReuseSmokeHome -DisableGuardian -SkipExtendedProbes -SessionCatalogUpgradeProbe "expect-recovered"
          & "apps/desktop/scripts/smoke-windows-desktop.ps1" -DesktopExePath $released044Exe -StartupTimeoutSec 120 -MaxReadySec 30 -ReuseSmokeHome -DisableGuardian -SkipExtendedProbes -SessionCatalogUpgradeProbe "expect-recovered"

          & "apps/desktop/scripts/smoke-windows-desktop.ps1" -DesktopExePath $pre047Exe -StartupTimeoutSec 120 -MaxReadySec 30 -DisableGuardian -SkipExtendedProbes -SessionCatalogUpgradeProbe "baseline-pre047"
          & "apps/desktop/scripts/smoke-windows-desktop.ps1" -DesktopExePath $broken047Exe -StartupTimeoutSec 120 -MaxReadySec 30 -ReuseSmokeHome -DisableGuardian -SkipExtendedProbes -SessionCatalogUpgradeProbe "seed-broken-047"

          # The isolated no-provider send path creates a placeholder catalog row
          # before the 0.47 event-index write fails. Affected upgraded homes
          # already have the inverse durable state: journal present, catalog row
          # absent, and migration marked complete. Materialize that exact state
          # before proving whether released 0.48 can repair it on a cold start.
          $sessionId = "windows-session-catalog-upgrade-regression"
          $journalRoot = Join-Path $env:RUNNER_TEMP "nextclaw-desktop-smoke-home/sessions/.ncp-agent-journal"
          $journalPath = Join-Path $journalRoot "$sessionId.jsonl"
          $catalogPath = Join-Path $journalRoot ".ncp-agent-session-catalog.sqlite"
          if (-not (Test-Path $journalPath) -or -not (Test-Path $catalogPath)) {
            throw "Released 0.47 persisted-state fixture is incomplete. journal=$journalPath catalog=$catalogPath"
          }
          $catalogStateScript = @'
          const { DatabaseSync } = require("node:sqlite");
          const [catalogPath, sessionId] = process.argv.slice(1);
          const database = new DatabaseSync(catalogPath);
          const migration = database.prepare("SELECT value FROM storage_meta WHERE key = ? LIMIT 1").get("migration_status");
          if (migration?.value !== "complete") throw new Error(`Expected completed migration, got ${JSON.stringify(migration)}`);
          database.prepare("DELETE FROM sessions WHERE session_id = ?").run(sessionId);
          const remaining = database.prepare("SELECT COUNT(*) AS count FROM sessions WHERE session_id = ?").get(sessionId);
          database.close();
          process.stdout.write(JSON.stringify({ migration: migration.value, remaining: Number(remaining.count) }));
          '@
          $catalogState = node -e $catalogStateScript $catalogPath $sessionId | ConvertFrom-Json
          if ($catalogState.migration -ne "complete" -or [int]$catalogState.remaining -ne 0) {
            throw "Failed to materialize the released 0.47 persisted failure state."
          }
          Write-Host "[desktop-smoke] materialized released 0.47 persisted failure state: migration=complete catalog=missing journal=present"

          & "apps/desktop/scripts/smoke-windows-desktop.ps1" -DesktopExePath $released048Exe -StartupTimeoutSec 120 -MaxReadySec 30 -ReuseSmokeHome -DisableGuardian -SkipExtendedProbes -SessionCatalogUpgradeProbe "expect-missing-048"
          & "apps/desktop/scripts/smoke-windows-desktop.ps1" -DesktopExePath $candidateExe -StartupTimeoutSec 120 -MaxReadySec 30 -ReuseSmokeHome -DisableGuardian -SkipExtendedProbes -SessionCatalogUpgradeProbe "expect-recovered"

      - name: Smoke test Windows desktop executable
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          if (-not (Test-Path "apps/desktop/release/win-unpacked/NextClaw Desktop.exe")) {
            throw "Windows desktop executable not found in apps/desktop/release/win-unpacked"
          }
          $desktopExe = (Resolve-Path "apps/desktop/release/win-unpacked/NextClaw Desktop.exe").Path
          & "apps/desktop/scripts/smoke-windows-desktop.ps1" -DesktopExePath $desktopExe -StartupTimeoutSec 120 -MaxReadySec 20 -SeedStaleSameVersionBundle -AllowRendererOnlyTitlebarProbe

      - name: Smoke test Windows Portable archive
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $PSNativeCommandUseErrorActionPreference = $true
          pnpm desktop:portable:verify -- --allow-renderer-only-titlebar-probe

      - name: Stage Windows smoke logs
        if: always()
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $stagingRoot = "apps/desktop/release/windows-smoke-logs"
          if (Test-Path $stagingRoot) {
            Remove-Item -Recurse -Force $stagingRoot
          }
          New-Item -ItemType Directory -Path $stagingRoot | Out-Null

          $homeLogs = Join-Path $env:RUNNER_TEMP "nextclaw-desktop-smoke-home/logs"
          if (Test-Path $homeLogs) {
            Copy-Item -Recurse -Force $homeLogs (Join-Path $stagingRoot "home-logs")
          }

          $smokeLogs = Join-Path $env:RUNNER_TEMP "nextclaw-desktop-smoke-logs"
          if (Test-Path $smokeLogs) {
            Copy-Item -Recurse -Force $smokeLogs (Join-Path $stagingRoot "smoke-logs")
          }

          $appLogs = Join-Path $env:APPDATA "NextClaw Desktop/logs"
          if (Test-Path $appLogs) {
            Copy-Item -Recurse -Force $appLogs (Join-Path $stagingRoot "app-logs")
          }

      - name: Upload Windows smoke logs artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: desktop-validate-smoke-logs-win32-x64
          if-no-files-found: warn
          path: |
            apps/desktop/release/windows-smoke-logs/**

      - name: Upload Windows desktop executable artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: desktop-validate-win32-x64-unpacked
          if-no-files-found: warn
          path: |
            apps/desktop/release/win-unpacked/**
            apps/desktop/release/NextClaw-Portable-*-win-x64.zip

  desktop-windows-installer-smoke:
    if: ${{ inputs.target == '' || inputs.target == 'all' || inputs.target == 'windows-installer' }}
    name: desktop-windows-installer-smoke
    runs-on: windows-latest
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.15.1

      - name: Install dependencies
        shell: pwsh
        run: pnpm install --frozen-lockfile

      - name: Build desktop runtime dependencies
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          pnpm -r --filter @nextclaw/desktop... build

      - name: Build Windows desktop installer
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $env:CSC_IDENTITY_AUTO_DISCOVERY = "false"
          pnpm -C apps/desktop bundle:seed -- --channel stable --platform win32 --arch x64
          pnpm -C apps/desktop lint
          pnpm -C apps/desktop tsc
          pnpm -C apps/desktop build:main
          pnpm -C apps/desktop native-resources -- --platform win32 --arch x64
          pnpm -C apps/desktop exec electron-builder --win nsis --x64 --publish never

      - name: Smoke test Windows desktop installer
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $installerPath = Get-ChildItem "apps/desktop/release" -Filter "*Setup*.exe" | Select-Object -First 1
          if ($null -eq $installerPath) {
            throw "Windows desktop installer not found in apps/desktop/release"
          }
          & "apps/desktop/scripts/smoke-windows-installer.ps1" -InstallerPath $installerPath.FullName -StartupTimeoutSec 120 -MaxReadySec 20 -SeedStaleSameVersionBundle -AllowRendererOnlyTitlebarProbe

      - name: Stage Windows installer smoke logs
        if: always()
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $stagingRoot = "apps/desktop/release/windows-installer-smoke-logs"
          if (Test-Path $stagingRoot) {
            Remove-Item -Recurse -Force $stagingRoot
          }
          New-Item -ItemType Directory -Path $stagingRoot | Out-Null

          $homeLogs = Join-Path $env:RUNNER_TEMP "nextclaw-desktop-smoke-home/logs"
          if (Test-Path $homeLogs) {
            Copy-Item -Recurse -Force $homeLogs (Join-Path $stagingRoot "home-logs")
          }

          $appLogs = Join-Path $env:APPDATA "NextClaw Desktop/logs"
          if (Test-Path $appLogs) {
            Copy-Item -Recurse -Force $appLogs (Join-Path $stagingRoot "app-logs")
          }

      - name: Upload Windows installer smoke logs artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: desktop-validate-smoke-logs-win32-x64-installer
          if-no-files-found: warn
          path: |
            apps/desktop/release/windows-installer-smoke-logs/**
            ${{ runner.temp }}/nextclaw-desktop-smoke-logs/**

      - name: Upload Windows desktop installer artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: desktop-validate-win32-x64-installer
          if-no-files-found: warn
          path: |
            apps/desktop/release/*Setup*.exe
            apps/desktop/release/latest.yml
            apps/desktop/release/*.exe.blockmap

  desktop-linux-package-smoke:
    if: ${{ inputs.target == '' || inputs.target == 'all' || inputs.target == 'linux-package' }}
    name: desktop-linux-package-smoke
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.15.1

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Linux packaging tools
        run: |
          sudo apt-get update
          sudo apt-get install -y apt-utils dpkg-dev gnupg

      - name: Build desktop runtime dependencies
        shell: bash
        run: |
          set -euo pipefail
          pnpm -r --filter @nextclaw/desktop... build

      - name: Build Linux AppImage + deb
        shell: bash
        run: |
          set -euo pipefail
          export CSC_IDENTITY_AUTO_DISCOVERY=false
          pnpm -C apps/desktop bundle:seed -- --channel stable --platform linux --arch x64
          pnpm -C apps/desktop lint
          pnpm -C apps/desktop tsc
          pnpm -C apps/desktop build:main
          pnpm -C apps/desktop native-resources -- --platform linux --arch x64
          pnpm -C apps/desktop exec electron-builder --linux AppImage deb --x64 --publish never

      - name: Normalize Linux artifact filenames
        shell: bash
        run: |
          set -euo pipefail
          bash apps/desktop/scripts/normalize-linux-release-artifacts.sh apps/desktop/release

      - name: Smoke test Linux AppImage
        shell: bash
        run: |
          set -euo pipefail
          APPIMAGE_PATH="$(find apps/desktop/release -maxdepth 1 -type f -name 'NextClaw.Desktop-*-linux-x64.AppImage' | head -n 1)"
          if [ -z "${APPIMAGE_PATH}" ]; then
            echo "Linux AppImage not found in apps/desktop/release"
            exit 1
          fi
          apps/desktop/scripts/smoke-linux-appimage.sh "${APPIMAGE_PATH}" 120

      - name: Smoke test Linux deb install
        shell: bash
        run: |
          set -euo pipefail
          DEB_PATH="$(find apps/desktop/release -maxdepth 1 -type f -name 'nextclaw-desktop_*.deb' | head -n 1)"
          if [ -z "${DEB_PATH}" ]; then
            echo "Linux deb not found in apps/desktop/release"
            exit 1
          fi
          bash apps/desktop/scripts/smoke-linux-deb.sh "${DEB_PATH}"

      - name: Build test-signed Linux APT repo
        shell: bash
        run: |
          set -euo pipefail
          node scripts/desktop/build-linux-apt-repo.mjs --input-dir apps/desktop/release --output-dir dist/linux-apt-repo --signing-mode test --github-pages-compatible

      - name: Smoke test Linux APT repo install
        shell: bash
        run: |
          set -euo pipefail
          bash apps/desktop/scripts/smoke-linux-apt-repo.sh dist/linux-apt-repo/apt

      - name: Upload Linux smoke logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: desktop-validate-smoke-logs-linux-x64
          if-no-files-found: warn
          path: |
            ${{ runner.temp }}/nextclaw-desktop-smoke-home/logs/**
            ${{ runner.temp }}/nextclaw-desktop-smoke-logs/**

      - name: Upload Linux package artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: desktop-validate-linux-x64-packages
          if-no-files-found: warn
          path: |
            apps/desktop/release/NextClaw.Desktop-*-linux-x64.AppImage
            apps/desktop/release/NextClaw.Desktop-*-linux-x64.AppImage.blockmap
            apps/desktop/release/nextclaw-desktop_*.deb
            dist/linux-apt-repo/**
