name: Source LOC Metrics

on:
  push:
    branches:
      - master
      - main
  pull_request:
  schedule:
    - cron: "15 2 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  code-volume:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc

      - name: Checkout OpenClaw benchmark repo
        uses: actions/checkout@v4
        with:
          repository: openclaw/openclaw
          path: external/openclaw
          fetch-depth: 1

      - name: Generate source LOC metrics
        run: |
          node scripts/metrics/code-volume-metrics.mjs \
            --append-history \
            --summary-file "$RUNNER_TEMP/code-volume-summary.md" \
            --benchmark-name openclaw \
            --benchmark-root external/openclaw \
            --benchmark-include-dirs src,extensions \
            --benchmark-output docs/metrics/code-volume/comparison.json

      - name: Generate Project Pulse data
        run: node scripts/project-pulse/generate-data.mjs

      - name: Commit source LOC snapshot
        if: (github.event_name == 'schedule' || github.event_name == 'workflow_dispatch') && (github.ref == 'refs/heads/master' || github.ref == 'refs/heads/main')
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "chore(metrics): update source loc snapshot [skip ci]"
          file_pattern: docs/metrics/code-volume/latest.json docs/metrics/code-volume/history.jsonl docs/metrics/code-volume/comparison.json apps/docs/.vitepress/data/project-pulse.generated.mjs apps/docs/public/project-pulse/gallery/*

      - name: Append workflow summary
        run: cat "$RUNNER_TEMP/code-volume-summary.md" >> "$GITHUB_STEP_SUMMARY"

      - name: Upload metrics artifact
        uses: actions/upload-artifact@v4
        with:
          name: source-loc-metrics-${{ github.run_id }}
          path: |
            docs/metrics/code-volume/latest.json
            docs/metrics/code-volume/history.jsonl
            docs/metrics/code-volume/comparison.json
            apps/docs/.vitepress/data/project-pulse.generated.mjs
            apps/docs/public/project-pulse/gallery
