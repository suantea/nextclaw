# v0.26.58 提供商模型目录发现与选择

## 迭代完成说明

- 支持目录发现的提供商设置页新增“获取模型列表”操作，用户可在保存前使用当前表单里的 API Base、API Key 与请求头获取模型；获取结果进入候选面板，不会自动同步，支持勾选若干模型“添加所选”，小目录也可“全部添加”。已知不提供目录接口的内建提供商不显示该操作，仍保留手工添加模型。
- 模型发现由 Core 统一负责协议与响应解析：OpenAI 兼容提供商读取 `/models`，Anthropic 读取并分页遍历 `/v1/models`，OpenCode Zen 按官方目录元数据排除已弃用和存在输入费用的模型，并进行五分钟进程内缓存。
- Core 在目录进入缓存和前端前统一筛选聊天 LLM：显式输出模态只接受纯文本输出，元数据缺失时按模型类型与稳定 ID 族排除图像、视频、语音、Embedding、Rerank 与 Moderation；图像输入、文本输出的视觉理解 LLM 不受影响。
- Server 只暴露一条 `POST /api/providers/:providerId/models/discover` 主链路；它复用连接测试的草稿配置解析，不持久化 API Key、API Base、请求头或获取结果。
- 前端保留本地模型的原有顺序和配置，仅追加远端新 ID 并去重。远端失败时展示可行动错误，用户仍可继续手动添加与保存。
- 获取模型前会检查当前草稿或已保存配置是否具备 API Key：未配置时直接提示先填写，不发送必然失败的请求；上游返回 401/403 时通过结构化状态显示本地化鉴权提示，不再把原始英文错误暴露给用户。
- 后台目录状态按当前 Provider 自己的条目收口，不再被其他 Provider 的全局刷新状态长期拖成“正在读取”；当前 Provider 已失败且没有可用快照时会显示本地化失败提示，并保留手工添加入口。
- 后台目录 Manager 对单个 Provider 设定 45 秒任务期限；即使底层请求或 Provider 实现没有响应取消信号，整轮刷新也会记录超时并结束 `refreshing`，前端轮询不会无限持续。
- Kernel 启动后立即刷新已启用提供商的模型目录，此后每 12 小时刷新一次进程内快照；自动刷新只更新厂商事实，不会静默新增、删除、重排或切换用户模型。
- 聊天模型选择器收起时不显示提醒；只有展开且存在候选时，才显示低优先级入口。候选默认显示“全部”，同时按提供商分段并支持提供商筛选；“添加”只补全配置，不切换当前会话模型。
- 进入具体提供商详情页时会自动读取该提供商的后台目录快照；后台快照与显式获取复用同一选择面板，支持添加所选与全部添加。操作只进入表单草稿，用户点击“保存”后才持久化；未保存连接参数仍通过“获取模型列表”读取对应目录。
- 已配置模型新增按需展开的批量删除模式，支持全选、取消全选、删除所选与取消；删除只修改当前表单草稿，不绕过页面统一保存入口。
- 设计依据：模型列表属于提供商能力发现，而不是静态 UI 常量；协议差异由 Core owner 收敛，Server 负责草稿配置边界，前端只负责触发、合并与反馈，从而避免为每个厂商复制一套交互和请求逻辑。
- 合同边界：获取模型就是获取目录，不发送测试消息，也不声称目录中的每个模型都通过了实时调用验证；账号权限、限额与服务健康属于独立的调用链路。
- 自动刷新、聊天选择器与提供商详情页的 owner 和交互边界见 [Provider 模型目录自动刷新与选择器发现设计](../../designs/2026-08-07-provider-model-catalog-refresh.design.md)。

## 测试/验证/验收方式

- Core 定向测试：覆盖 OpenAI 兼容响应、Anthropic 分页、OpenCode 免费筛选与缓存、HTTP/空响应/网络失败。
- Kimi 能力补充验收：Provider meta 定向测试确认标准 `moonshot` 出现在发现白名单且 `kimi-coding` 继续禁用；Core OpenAI-compatible 目录协议测试、Runtime/Server TypeScript、包级 ESLint、全仓治理与 backlog ratchet 均通过。真实请求 `https://api.moonshot.ai/v1/models` 返回 Kimi 的规范鉴权错误，证明端点路径成立；本机未配置 Kimi API Key，因此未读取真实账号模型数组。
- Server 组装路由测试：覆盖 OpenCode 无用户密钥、未保存草稿透传且不持久化、404 与上游错误合同。
- 55 个定向测试通过：覆盖 Core 三类目录协议、Kernel 自动刷新、Server 路由合同、聊天选择器分组，以及设置页获取后不自动添加、添加所选和全部添加。
- Provider 能力白名单初次纠偏时 31 个定向测试通过：覆盖 Core 要求内建 Provider 显式加入能力白名单、Kernel 跳过未审计 Provider、Server 当时精确投影 10 个支持与 9 个隐藏项，以及前端差集、全量已添加状态和隐藏入口；后续 Kimi 补充验收将当前投影更新为 11 个支持与 8 个隐藏项。
- 七个受影响包的 TypeScript 检查、完整源码构建、定向 lint、文档站构建和本次功能范围的仓库治理检查均通过。
- 使用隔离临时 HOME 启动当前源码构建实例，并通过真实 HTTP 分别验证 OpenRouter、NextClaw 公共网关与 OpenCode Zen。
- 使用真实浏览器点击“获取模型列表”：测试目录包含 8 个、当前草稿已有 4 个时，界面准确显示 4 个候选；勾选其中 2 个并“添加所选”后只把目标模型加入草稿，另 2 个继续保留为候选，保存按钮变为可用，而磁盘配置仍保持原来的 4 个。
- 自动目录 Manager 定向测试覆盖启动立即刷新、12 小时周期、并发刷新合并、只刷新已启用提供商、配置不变、单提供商失败保留最近成功目录，以及 dispose 清理定时器。
- 隔离临时 HOME 启动当前源码：全新配置未设置 OpenCode API Key，默认配置包含 7 个模型；官方实时元数据含 26 个零价格条目，其中 18 个已标记 `deprecated`，按 OpenCode 自身规则过滤后目录为 8 个，聊天选择器准确显示唯一候选 `ling-3.0-tiny-free`。
- 聊天 UI 定向测试覆盖收起无提醒、展开提示、“全部 / 提供商”分组、长模型 ID 弹性截断与固定操作列，以及添加动作不主动关闭面板。
- 提供商设置 UI 定向测试覆盖后台首次检查、对应 provider 候选差集、显式获取不自动添加、添加所选 / 全部添加到草稿、长 ID 布局和保存提示。
- 普通 DashScope 能力诊断：阿里云官方 OpenAI 兼容文档只承诺推理接口，并把可用模型引导到静态模型大全；未找到普通 DashScope `GET /models` 的官方合同，真实请求也返回 401。Coding Plan 端点返回 200 只能证明该独立端点响应，不能反向判断用户密钥类型；其官方页面给出的是专属 Base、专属 Key 与静态模型白名单，未形成与当前认证合同匹配的动态目录证据。因此 `dashscope` 与 `dashscope-coding-plan` 均关闭目录发现。
- 内建 Provider 改为显式能力白名单。当前开放 NextClaw、OpenAI、Anthropic、OpenRouter、DeepSeek、MiniMax API、Kimi、AiHubMix、Groq、OpenCode Zen 与 vLLM；Gemini、DashScope、DashScope Coding Plan、Kimi Coding、Zhipu AI、Xiaomi MiMo、MiniMax Portal 与 Qwen Portal 均隐藏入口并跳过后台刷新。自定义兼容 Provider 仍保留高级尝试入口。
- Kimi 官方已明确提供与当前 `https://api.moonshot.ai/v1` Base URL、Bearer 认证及 OpenAI 列表响应兼容的 `GET /models` 合同，因此标准 `moonshot` Provider 接入既有 `openai-compatible` 目录发现主链路；`kimi-coding` 仍使用独立 Base URL 与 Anthropic Messages 协议，不随之开放。
- 隔离临时 HOME 的当前源码真实验收：`/api/provider-templates` 精确返回上述 11 个 `supportsModelDiscovery=true` 与 8 个 `false`。真实浏览器新增普通 DashScope 和 DashScope Coding Plan 后，两者的模型区域都只有“添加模型”，没有“获取模型列表”、自动发现或批量添加；OpenCode 与 OpenRouter 则正常显示入口并取得目录。
- 真实浏览器差集验收：全新实例的 OpenCode 默认已有 7 个模型，上游实时目录返回 8 个；点击“获取模型列表”后只显示唯一可添加项 `ling-3.0-tiny-free`，候选面板自动展开、滚动到 700px 视口内并获得焦点。点击“全部添加”后，界面立即变为“已获取 8 个模型，当前列表已全部包含”，不再显示空选择器或添加按钮；保存、强制刷新页面并再次获取后仍保持相同结果。
- 显式获取的 toast 现在只说明“模型列表获取成功”，不再把上游总量当作可添加数量，也不再提示用户寻找不存在的选择入口；可添加数量与全量已包含状态统一由差集面板表达。
- 大目录体验纠偏：完整目录改为浏览能力，更新提示只表示“上次已见之后新增”。单 Provider 首次候选超过 50 个时建立本机 UI 基线，不把数百项既有目录持续当作新模型；小批候选可通过“本批不再提醒”写入已见状态，Provider 配置不发生变化。
- Provider 页候选超过 50 个时不展示“全部添加”，提供搜索并最多渲染前 50 个匹配项。先以 OpenRouter 388 项原始目录完成大目录交互验收：页面没有全部添加，搜索精确模型后只添加所选 1 个，其他模型未进入草稿；最终聊天 LLM 筛选后的实时目录为 382 项，同一大目录约束继续生效。
- 聊天选择器真实验收：清空提醒状态后首次读取大目录时不显示数百条既有模型提醒并在本机建立基线；最终实时目录中模拟 3 个未见候选后，选择器只显示“发现 3 个”。点击“本批不再提醒”后提示立即消失，Provider 模型配置保持不变。
- Core 混合目录定向测试覆盖 Agnes 图像/视频模型、Embedding、BGE、Rerank、多输出模态、安全分类、Guard 与视觉理解 LLM：这些非聊天模型不会返回，图像输入但只输出文本的 LLM 与普通文本 LLM 保留。OpenRouter 实时快照最终返回 382 项，明确排除 Gemini Image、GPT Audio、Nemotron Content Safety、Llama Guard 与 GPT Safeguard，同时保留 Qwen3-VL 和 Claude Sonnet。
- 聊天选择器布局改为搜索栏和管理入口固定、中部模型与发现候选共用单一滚动区；组件测试明确断言发现候选属于中部滚动区，底部“管理模型和提供商”位于滚动区外且不可收缩。真实浏览器在 1000×600 窗口中确认中部滚动区可滚动、发现候选位于该滚动区内，底部管理入口完整落在面板边界内且不可收缩。
- Provider 模型区定向测试新增批量删除验收：进入选择模式、全选、取消全选、选择两个模型并删除后，只把未选模型传回草稿，同时退出选择模式。真实浏览器从 OpenCode 的 8 个模型中勾选 2 个批量删除后，页面显示 6 个并启用保存，但 API 仍返回持久化的 8 个；未保存刷新后 8 个模型完整恢复。
- 可维护性收口把 `provider-form.tsx` 从 636 行降到 342 行，详情视图独立为 306 行；同时删除两个用 Effect 修补表单草稿的旧路径，改由配置快照显式重建编辑器，并在模型批量变更时同步规范化模型配置。重构后的源码实例再次通过相同的 8→6→未保存刷新恢复 8 个模型的浏览器验收。

## 发布/部署方式

- 本次已添加七个受影响 NPM 包的 patch changeset，后续随统一 NPM 发布进入产品。
- 本次收尾包含本地提交；未执行推送、NPM 发布、线上部署或服务重启。
- 不涉及数据库 migration、远程配置迁移或桌面安装器变更。

## 用户/产品视角的验收步骤

1. 打开“设置 → 提供商”，选择一个支持目录发现的已配置或正在编辑提供商；普通 DashScope 应只显示手工“添加模型”，不显示“获取模型列表”。
2. 可先修改 API Base、API Key 或自定义请求头，但不要保存。
3. 点击模型列表区域的“获取模型列表”，等待候选面板自动展开。
4. 确认获取后原有模型、保存按钮和后端配置都不变化；勾选若干候选后点击“添加所选”，或按需“全部添加”。
5. 确认只有明确添加的模型进入列表末尾，重复 ID 不会出现两次，并且“保存”按钮变为可用；刷新页面前，后端配置仍是旧值，只有主动点击“保存”才会持久化。
6. 将 API Base 临时改为不可达地址后再次获取，确认出现明确错误且仍可手动添加模型。
7. 在全新安装环境中无需填写 OpenCode API Key；启动完成后打开聊天模型选择器，确认收起时无提醒、展开后有“全部”和对应提供商筛选，长模型名不会覆盖“添加”按钮。
8. 添加任一候选，确认当前会话模型不变、选择器保持打开，候选在配置刷新后消失。
9. 进入“设置 → 提供商 → OpenCode Zen Free Trial”，确认页面自动提示对应候选；勾选部分后“添加所选”或“全部添加”，确认“保存”按钮变为可用，刷新页面前后端配置仍未被自动改写。

## 可维护性总结汇总

- `post-edit-maintainability-guard --paths <本次功能范围>`：通过，检查最终 18 个核心改动文件，0 error、2 warning；warning 分别是 `chat-input-bar-toolbar.tsx` 当前 426 行、接近 500 行预算，以及配套测试随新增交互增长 150 行，均未形成治理错误。
- 提交范围代码增减报告：76 个文件共新增 5235 行、删除 966 行、净增 4269 行；其中生产代码新增 3180 行、删除 869 行、净增 2311 行，测试新增 1691 行、删除 97 行、净增 1594 行，其余为设计、用户文档、changeset、迭代记录与调查规则。本次属于新增用户能力，净增长主要来自三类协议解析、自动目录 owner、完整路由合同、聊天与设置页交互和覆盖测试。
- 全工作区治理还包含其他在途任务的变更，因此最终可维护性守卫按本次功能的受控路径执行；提交前另做双向范围审计，确保不把无关改动混入本次提交。
- 正向减债动作：连接测试与模型发现共享 `ProviderConnectivityService` 的草稿配置解析；各厂商差异通过声明式 `modelDiscovery` 合同进入 Core 单一路径，没有在 Controller 或 React 组件里复制协议解析。
- Server 配置大文件减少 176 行，连接测试及模型发现网络编排迁出；`provider-form.tsx` 从 636 行降到 342 行，详情视图迁入 306 行的独立组件，并删除两个用 Effect 修补表单草稿的旧路径。
- 主观可维护性复核：通过。新增 Core service、Server service 和 UI hook 分别对应协议、服务端草稿配置、界面编排三个真实变化点；没有平行请求路径、隐式自动保存或 provider-specific UI 分支。
- 已知边界：模型目录由外部厂商维护，网络、鉴权、限额和接口支持情况可能变化；失败保持显式，不将旧列表静默替换或自动保存。

## 纠偏复盘与规则落地

- 纠偏来源：用户明确指出当前密钥不是 Coding Plan，并提出“不支持就不要提供操作”。此前把 Coding Plan `/models` 的 200 外推成密钥类型判断，属于跨操作、跨端点推断。
- 失误模式：把“同品牌、兼容 OpenAI、另一个端点成功”错误等同于“目标目录端点受支持”。现有代码调查规则要求走完整链路，但没有明确约束外部协议必须按具体操作逐项取证，因此没有及时拦住这类端点能力外推。
- 方法级修复：在 `code-investigation-workflow` skill 增加通用规则——端点 A、SDK 兼容声明或共享 Base URL 都不能证明端点 B；未证实能力不得生成 UI 操作，必须由 capability 合同控制。该规则会在今后研究外部源码、协议和厂商兼容性时自动触发，而不是只记住 DashScope 这一例。
- 产品与代码修复：`ProviderSpec.modelDiscovery=false` 成为 Core 的明确禁用合同；Kernel、自动刷新、Server 元数据和设置页统一消费该事实。测试从网络 owner、调度 owner、API 投影到 React 展示逐层覆盖。没有新增静态治理脚本，因为外部厂商能力会变化，静态扫描无法可靠判断；正确的自动约束是 capability 合同与回归测试。

## 红区触达与减债记录

### packages/nextclaw-server/src/features/config/stores/server-config.store.ts

- 本次是否减债：是。
- 说明：把连接测试、模型发现、草稿配置归一化与测试模型解析整体迁到 `ProviderConnectivityService`，该热点文件相对改动前减少 176 行；Store 回到配置读写与投影职责，不再直接负责提供商网络编排。
- 下一步拆分缝：继续按 provider、channel、runtime 三个域迁出配置构建与默认值归一化，最终让 Store 只保留持久化和聚合入口。

### packages/nextclaw-ui/src/features/settings/components/config/provider-form.tsx

- 本次是否减债：是。
- 说明：把连接测试与模型发现的异步编排迁入专用 hook，把详情展示迁入 `provider-form-detail-pane.tsx`；表单壳从 636 行降到 342 行，并以配置快照显式重建编辑器，删除 Effect 修补草稿的平行路径。
- 下一步拆分缝：继续按认证、模型、基础配置收敛详情面板的 presenter 数据，降低跨区域 props 合同体积。

## NPM 包发布记录

- `@nextclaw/core`：已添加 patch changeset，待统一发布。
- `@nextclaw/runtime`：已添加 patch changeset，待统一发布。
- `@nextclaw/kernel`：已添加 patch changeset，待统一发布。
- `@nextclaw/server`：已添加 patch changeset，待统一发布。
- `@nextclaw/client-sdk`：已添加 patch changeset，待统一发布。
- `@nextclaw/ui`：已添加 patch changeset，待统一发布。
- `@nextclaw/agent-chat-ui`：已添加 patch changeset，待统一发布。
- 当前未执行 NPM 发布。
