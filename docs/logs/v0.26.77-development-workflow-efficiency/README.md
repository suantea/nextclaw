# v0.26.77 开发流程效率治理

日期：2026-08-08

## 迭代完成说明

本轮重构 NextClaw 的默认开发流程规则，目标是在不降低正确性底线的前提下，减少重复读规则、重复验证、低信息量工具调用和无条件二次复核。

根因不是某一次任务“做得太认真”，而是多层规则长期叠加后，把原本应按风险选择的动作写成了普遍必做项：`AGENTS.md`、交付、干净实现、验证、可维护性 guard、主观 review 和迭代留痕分别重复约束同一阶段；硬性代码净增长门槛还可能驱动 AI 为了过线搜索无关删减点。规则越多，AI 每轮需要读取和执行的上下文越长，验证也越容易重复。

本轮完成：

- 将默认开发任务划分为 L0-L4 风险级别，验证强度跟随行为风险，而不是跟随固定清单。
- 将 TypeScript 类型检查保留为类型边界的硬要求；将包级 lint、治理 ratchet、运行冒烟和主观可维护性 review 改为条件触发。
- 纯视觉、文案和主观效果优先使用渲染检查与用户验收；交互、状态、数据和协议行为继续由 AI 提供自动化或运行证据。
- 将复杂 UI、runtime update 和 release 细则移入验证 skill 的延迟参考，普通任务不再预读。
- 删除普通修复和重构的强制“生产语义代码净增小于等于零”；必要且清晰的最小增长允许存在，显式零增长治理战役仍保留专门合同。
- 将首次治理 preflight 限定为新增、重命名、移动文件，或改变角色与目录边界的任务。
- 可维护性 guard 默认只跑一次；主观 review 只在 guard 告警、结构/owner/抽象变化、大范围改动或用户明确要求时运行。
- 将流程效率反思嵌入既有收尾，不新增独立仪式。AI 需要检查重复读取、重复验证、低信息量动作和可条件化的规则；只有可复用、反复发生或高影响的结论才持久化。
- 普通局部修复和同批次微调不再自动创建独立迭代记录。
- 对项目全部 skill 做了第二阶段结构审计：删除无独立价值或已被常驻规则覆盖的入口，将文件组织、goal、Marketplace、desktop release、release notes、NPM release、Linear 委派等分支型长流程收敛为小入口加条件 reference。
- 普通开发只保留 `nextclaw-delivery-workflow` 一个默认流程 owner；调查、方案、验证和 maintainability guard 均在对应阶段按需进入，专项 skill 不再回链上游 workflow。
- 新增 `pnpm check:skill-progressive-loading`，只在元规则或 skill 分层变化时检查 frontmatter、名称唯一性、相对链接、退役引用、依赖循环和体积预算，不进入普通开发默认验证链路。
- 同步迁移仍可能被执行的 `docs/designs` / `docs/plans` 中旧 skill 名称与脚本路径；历史 `docs/logs` 保留当时事实，不作为运行期规则入口。

以字节数近似衡量规则输入体积：高频流程规则包由约 110 KB 降至 48 KB，减少约 56%；包含条件复核、留痕与延迟参考的完整相关规则包由约 127 KB 降至 60 KB，减少约 53%。这不是模型实际 token 的精确换算，但能稳定反映默认上下文负担的下降。

上一轮结构审计后，项目 skill 数由 56 降至 44，全部 `SKILL.md` 入口正文由约 466 KB 降至约 211 KB，description 总字符由 9610 降至 6035；`AGENTS.md` 由约 31 KB 降至 8814 字节。静态依赖循环由 4 个组件降至 0。包括 references 在内的全部 skill Markdown 也从 489907 字节降至 284775 字节，减少 41.9%。

本次续审按“一个标准开发流程 + 按需复杂阶段 + 完整场景流程 + 条件工艺/场景规范”重新分类，又把 11 个没有独立任务或阶段价值的入口并入所属 owner。最终 skill 数为 33，`SKILL.md` 总量 122098 字节，description 总字符 3830，全部 skill Markdown 217539 字节，最大入口 7459 字节，依赖循环仍为 0。入口正文较上一轮再下降 42.1%，且迁入的 reference 均删除重复触发、上游流程复述和互相回链，不是机械搬家。

## 测试/验证/验收方式

- `git diff --check`：验证 Markdown 与规则改动不存在空白错误。
- 校验所有改写 skill 的 frontmatter，以及验证 skill 新增延迟参考链接均可解析。
- `pnpm check:governance-backlog-ratchet`：验证治理基线没有因规则重构发生非预期回退。
- `node --test scripts/governance/checks/skill-progressive-loading.test.mjs`：验证新检查能识别循环、失效链接、重复名称和退役引用。
- `pnpm check:skill-progressive-loading`：验证 33 个 skill 的 frontmatter、链接、引用图、退役名称与收紧后的体积/数量预算，输出结果为 0 个违规。
- 人工一致性检查 `AGENTS.md`、`commands/commands.md` 与各流程 skill，确认默认路径、风险分级、条件 review 和留痕边界没有互相冲突。
- 搜索非历史规则、设计和计划中的退役 skill 名称，仅剩普通文档 `file-naming-convention.md` 文件名引用，不再存在已删除 skill 或脚本路径引用。

本轮没有触达 TypeScript、产品运行链路或用户运行行为，因此 build、tsc 和产品冒烟不适用；新增的治理脚本执行定向 lint 与 Node 回归测试。

## 发布/部署方式

前一阶段与本次同批次 skill 治理均按用户要求提交到当前本地分支。未推送、未部署、未发布。

## 用户/产品视角的验收步骤

1. 用一个纯文案或纯视觉微调任务观察 AI：应选择 L0/L1，不执行全套治理和运行冒烟。
2. 用一个局部交互修复观察 AI：应选择 L2，运行定向测试和必要类型检查，最终只重复一次关键验证。
3. 用一个跨包协议或运行链路改动观察 AI：应选择 L3，并保留类型、契约和贴近真实链路的证据。
4. 查看最终汇报：AI 应说明实际执行和跳过的验证，但不应为了展示完整性堆砌无信息量命令。
5. 查看收尾反思：只有发现可复用的流程问题时才提出规则优化，普通任务不新增单独的“token review”步骤。

## 可维护性总结汇总

- 本轮主要是删除、合并和条件化重复规则，没有新增平行流程 owner。
- 高频规则输入体积减少约 56%，完整相关规则包减少约 53%。
- 新增的三个参考文件只承载低频复杂场景，避免默认 skill 继续膨胀。
- 上一轮合并 12 个冗余入口，本次续审再合并 11 个；当前低频细节保存在 42 个条件 reference 中，没有以丢失合同换取入口缩短。
- 可维护性 guard 与主观 review 的职责已分开：guard 提供低成本客观信号，review 只处理需要判断的风险。
- 新检查为 `AGENTS.md`、单个/全部 skill 入口、入口数量和 description 设预算，并阻止依赖回路与 reference 伪装 skill frontmatter；数量预算用于阻止无审计增长，不替代新入口的职责判断。
- 本轮属于大型治理重构，因此创建一次迭代记录；没有为过程中每个细小修正分别留痕。

## NPM 包发布记录

不适用。本轮未修改 NPM 包源码、版本、依赖、产物或发布合同。
